import 'package:flutter/material.dart';

class FooterAppView extends StatelessWidget {
  const FooterAppView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
